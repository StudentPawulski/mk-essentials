<h3 class="title is-4"><%= @category.name %></h3>

<h4 class="subtitle is-5">Products</h4>

<div class="columns is-multiline">
  <%= render partial: 'products/product',
            collection: @category.products,
            locals: { name_is_link: true } %>
</div>